name: test
on:
  push:
permissions:
  contents: read
jobs:
  install:
    strategy:
      matrix:
        os:
          - macos-12
          - ubuntu-22.04
    runs-on: ${{matrix.os}}
    steps:
      - uses: actions/checkout@v3
      - name: Install
        run: |
          sh -c "$(curl -fsLS https://chezmoi.io/get)" -- init --apply --branch ${GITHUB_REF_NAME} m1yam0t0
          rm -rf $(pwd)/bin
